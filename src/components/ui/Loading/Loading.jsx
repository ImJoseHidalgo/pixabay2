import React from 'react';
import { LoadingStyles } from './Loading.styles';

const Loading = () => {
  return (
    <LoadingStyles>
      <h1>Cargando...</h1>
    </LoadingStyles>
  )
}

export default Loading;
